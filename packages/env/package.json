{
    "private": true,
    "name": "@jshare/env",
    "version": "1.0.0",
    "description": "JShare environment config",
    "license": "MIT",
    "main": "build/index.js",
    "scripts": {
        "lint": "eslint --ext js,ts,tsx .",
        "build": "tsup --clean",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@jshare/eslint-config": "workspace:*",
        "@jshare/tsconfig": "workspace:*"
    },
    "eslintConfig": {
        "extends": "@jshare/eslint-config",
        "ignorePatterns": [
            "node_modules",
            "build"
        ]
    }
}
