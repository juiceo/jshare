doctype html
html
    head
        title JShare | Invalid invite link
        meta(name="description" content="No group was found with this link. Please ask the group owner for a new link.")
        meta(name="apple-itunes-app" content="app-id=6743673035")

        link(rel="preconnect" href="https://fonts.googleapis.com")
        link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
        link(href="https://fonts.googleapis.com/css2?family=Open+Sans:ital@0;1&family=Poppins:wght@400;500&display=swap" rel="stylesheet")
        link(rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png")
        link(rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png")
        link(rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png")
        link(rel="manifest" href="/site.webmanifest")

    style.
        * {
            box-sizing: border-box;
            margin: 0;
        }

        body {
            padding: 16px;
            font-family: Open Sans;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            background-color: #FFA5A5;
        }

        .wrapper {
            width: 100%;
            max-width: 600px;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: blue;
            border-radius: 6px;
            background-color: #ececec;
            padding: 32px;
            gap: 8px;
            box-shadow: 0px 3px 15px rgba(0,0,0,0.2);
            animation: fadeIn 0.5s cubic-bezier(0.25, 0.1, 0.25, 1.0);
        }

        h1, h3 {
            font-family: Poppins;
        }

        img {
            border-radius: 50%;
            border: 2px solid #FFA5A5;
        }

        button {
            background-color: #254459;
            color: white;
            border: none;
            padding: 8px;
            min-width: 200px;
            border-radius: 6px;
            font-weight: 500;
            cursor: pointer;
            font-weight: 500;
            transition: background-color 0.2s ease-in-out;
        }

        button:hover {
            background-color: #557687;
        }

        .description {
            opacity: 0.8;
            font-size: 14px;
            margin: 40px 0;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0px);
            }
        }
        
    body
        div.wrapper
            img(src="/jshare.png" width="100" height="100")
            h1 Invalid link
            p.description Looks like this invite link is invalid or expired. Please ask the group owner for a new link.