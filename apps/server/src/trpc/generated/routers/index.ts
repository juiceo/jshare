/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import type { AnyTRPCRouter as AnyRouter } from '@trpc/server';

import type { PrismaClient } from '../../../../../../packages/db/src/generated/models';
import { createTRPCRouter } from '../../trpc';
import createExchangeRatesRouter from './ExchangeRates.router';
import createExpenseRouter from './Expense.router';
import createExpenseShareRouter from './ExpenseShare.router';
import createGroupRouter from './Group.router';
import createGroupParticipantRouter from './GroupParticipant.router';
import createImageRouter from './Image.router';
import createMessageRouter from './Message.router';
import createMessageAttachmentRouter from './MessageAttachment.router';
import createPaymentRouter from './Payment.router';
import createProfileRouter from './Profile.router';

export function db(ctx: any) {
    if (!ctx.prisma) {
        throw new Error('Missing "prisma" field in trpc context');
    }
    return ctx.prisma as PrismaClient;
}

export function createRouter() {
    return createTRPCRouter({
        profile: createProfileRouter(),
        group: createGroupRouter(),
        groupParticipant: createGroupParticipantRouter(),
        image: createImageRouter(),
        message: createMessageRouter(),
        messageAttachment: createMessageAttachmentRouter(),
        expense: createExpenseRouter(),
        expenseShare: createExpenseShareRouter(),
        payment: createPaymentRouter(),
        exchangeRates: createExchangeRatesRouter(),
    });
}
